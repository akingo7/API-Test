apis:
  - url: https://hfappprod.hydrafacial.com
    paths:
      - path: /gift-card-usage/list/v2
        auth: "No" # Takes one of ["Special", "Basic", "No"]
        requests: ["GET"]
  - url: https://reqres.in
    paths:
      - path: /api/login
        auth: "No" # Takes one of ["Special", "Basic", "No"]
        requests: ["POST"]
        