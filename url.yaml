apis:
  - url: https://reqres.in
    paths:
      - path: /
        auth: "No" # Takes one of ["Special", "Basic", "No"]
        requests: ["GET"]
  - url: https://reqres.in
    paths:
      - path: /api/login
        auth: "No" # Takes one of ["Special", "Basic", "No"]
        requests: ["POST"]
        